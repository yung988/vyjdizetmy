/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 15+ automatically uses src/app if /app doesn't exist
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
};

export default nextConfig;
